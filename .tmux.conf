# Reload Tmux
bind r source-file ~/.tmux.conf

# Enable Mouse
set -g  mouse on

# Delay between key press
set -s escape-time 0

# Enable hjkl instead of arrow key
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

